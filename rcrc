# vim: ft=sh
EXCLUDES=README.md

if [ "$(uname)" != Darwin ]; then
    EXCLUDES="Library:* $EXCLUDES"
else
    UNDOTTED=Library
fi

SYMLINK_DIRS="\
    config/fish \
    config/nvim \
    config/Code/User \
    tmux/plugins/tpm \
"
DOTFILES_DIRS="\
    ~/.dotfiles \
    ~/.dotfiles-corp \
"

if [ -e ~/.dotfiles-corp/rcrc ]; then
    . ~/.dotfiles-corp/rcrc
fi

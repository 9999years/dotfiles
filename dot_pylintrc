[MESSAGES CONTROL]
# Disable "can't import..." message
# Disable "else after if/return"
disable = E0401, R1705

[BASIC]

good-names=i,
    j,
    k,
    ex,
    Run,
    f,
    fp,
    _

# One- or two-letter names. Use responsibly!
good-names-rgxs=[a-zA-Z][a-zA-Z]?

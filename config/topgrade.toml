# https://github.com/r-darwish/topgrade/blob/master/config.example.toml

[commands]
"Update fundle" = "fish -c 'fundle self-update && fundle update'"
"Update Neovim/Packer" = "nvim -c 'autocmd User PackerComplete quitall' -c 'PackerSync'"
